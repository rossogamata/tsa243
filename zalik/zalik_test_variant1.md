# ЗАЛІК — ВАРІАНТ 1
## Технології системного адміністрування | Курс 2-й | ВІТІ

> **Дата:** _________________ | **Прізвище, ім'я:** _________________________________  
> **Інструкція:** Оберіть одну правильну відповідь на кожне питання.  
> **Оцінювання:** 30 питань × 2 бали = **60 балів**

---

## БЛОК 1 — Процеси та файлові операції

**1.** Яка команда виводить дерево процесів у Linux?

- A) `ps aux`
- B) `ps -ejH`
- C) `top -H`
- D) `pstree -n`

---

**2.** Яка команда надсилає процесу з PID 1234 сигнал SIGKILL (примусове завершення)?

- A) `kill -1 1234`
- B) `kill -15 1234`
- C) `kill -9 1234`
- D) `kill -2 1234`

---

## БЛОК 2 — Файлова система та диски

**3.** Яка команда відображає структуру дисків разом з назвами файлових систем і UUID?

- A) `fdisk -l`
- B) `df -hT`
- C) `lsblk -f`
- D) `mount`

---

**4.** Який файл потрібно відредагувати, щоб розділ монтувався автоматично при кожному завантаженні системи?

- A) `/etc/mtab`
- B) `/etc/fstab`
- C) `/etc/grub.cfg`
- D) `/proc/mounts`

---

## БЛОК 3 — Керування пакетами та ПЗ

**5.** Яка команда повністю видаляє пакет разом з його конфігураційними файлами?

- A) `apt remove <pkg>`
- B) `apt purge <pkg>`
- C) `dpkg --delete <pkg>`
- D) `apt uninstall <pkg>`

---

**6.** Яка команда показує, якому встановленому пакету належить файл `/usr/bin/git`?

- A) `apt show git`
- B) `dpkg -L git`
- C) `dpkg -S /usr/bin/git`
- D) `apt-cache search git`

---

**7.** Яка команда відображає перелік динамічних бібліотек (shared libraries), від яких залежить бінарний файл?

- A) `ldconfig -p`
- B) `ldd`
- C) `nm -D`
- D) `objdump -p`

---

## БЛОК 4 — Користувачі, групи, планування, час

**8.** Яку команду слід використовувати для безпечного редагування файлу `/etc/sudoers`?

- A) `nano /etc/sudoers`
- B) `vim /etc/sudoers`
- C) `visudo`
- D) `sudo vi /etc/sudoers`

---

**9.** В якому файлі Linux зберігаються хешовані паролі користувачів?

- A) `/etc/passwd`
- B) `/etc/shadow`
- C) `/etc/security/passwd`
- D) `/etc/login.defs`

---

**10.** Який запис crontab виконує скрипт `/opt/check.sh` кожні 15 хвилин?

- A) `15 * * * * /opt/check.sh`
- B) `*/15 * * * * /opt/check.sh`
- C) `* */15 * * * /opt/check.sh`
- D) `0,15,30,45 1 * * * /opt/check.sh`

---

## БЛОК 5 — Bash-скрипти

**11.** Яка команда перевіряє синтаксис bash-скрипту без його виконання?

- A) `bash -x script.sh`
- B) `bash -v script.sh`
- C) `bash -n script.sh`
- D) `bash --check script.sh`

---

**12.** Що зберігає спеціальна змінна `$?` у bash?

- A) PID поточного скрипту
- B) Кількість аргументів командного рядка
- C) Код завершення (exit code) останньої виконаної команди
- D) Ім'я поточного скрипту

---

## БЛОК 6 — Логування

**13.** Яка команда виводить журнали сервісу `ssh` через systemd-journald?

- A) `journalctl /var/log/syslog`
- B) `journalctl -u ssh`
- C) `tail -f /var/log/auth.log`
- D) `syslogd --service ssh`

---

**14.** Яка утиліта Linux відповідає за ротацію та стиснення лог-файлів?

- A) `logwatch`
- B) `rsyslogd`
- C) `logrotate`
- D) `syslog-ng`

---

## БЛОК 7 — Мережева конфігурація

**15.** Яка команда показує всі активні TCP-з'єднання у стані LISTEN разом з іменами процесів?

- A) `netstat -a`
- B) `ss -tlnp`
- C) `ip link show`
- D) `lsof -i tcp`

---

**16.** Яка утиліта використовується для налаштування мережевих інтерфейсів в Ubuntu 24.04 LTS?

- A) `network-manager`
- B) `ifconfig`
- C) `netplan`
- D) `nmcli`

---

**17.** Яка команда виконує ping-scan підмережі `192.168.177.0/24` (виявлення активних хостів без сканування портів)?

- A) `nmap -sV 192.168.177.0/24`
- B) `nmap -sn 192.168.177.0/24`
- C) `ping 192.168.177.0/24`
- D) `nmap -O 192.168.177.0/24`

---

## БЛОК 8 — SSH, SCP, Rsync

**18.** Яка команда генерує пару ключів SSH типу Ed25519?

- A) `ssh-keygen -t rsa -b 4096`
- B) `ssh-keygen -t ed25519`
- C) `openssl genrsa -out id_ed25519`
- D) `ssh-keygen -t ecdsa`

---

**19.** Яка команда копіює публічний ключ SSH до файлу `~/.ssh/authorized_keys` на віддаленому сервері?

- A) `scp ~/.ssh/id_ed25519.pub user@host:/`
- B) `ssh-add ~/.ssh/id_ed25519.pub`
- C) `ssh-copy-id user@host`
- D) `ssh-agent user@host`

---

## БЛОК 9 — Протокол HTTP

**20.** Який метод HTTP є ідемпотентним, але НЕ безпечним (тобто змінює стан сервера)?

- A) `GET`
- B) `HEAD`
- C) `DELETE`
- D) `OPTIONS`

---

**21.** Який HTTP-код відповіді означає постійне перенаправлення ресурсу на нову адресу?

- A) `302 Found`
- B) `304 Not Modified`
- C) `301 Moved Permanently`
- D) `307 Temporary Redirect`

---

**22.** Який атрибут cookie захищає від XSS-атак, роблячи cookie недоступним для JavaScript?

- A) `Secure`
- B) `SameSite=Strict`
- C) `HttpOnly`
- D) `Path=/`

---

## БЛОК 10 — Протокол TLS/SSL

**23.** Що таке Perfect Forward Secrecy (PFS) у контексті TLS?

- A) Шифрування даних симетричним алгоритмом AES
- B) Властивість, при якій компрометація приватного ключа сервера не дозволяє розшифрувати минулий трафік завдяки використанню тимчасових ключів для кожного з'єднання
- C) Перевірка статусу сертифіката через OCSP
- D) Стиснення заголовків у протоколі HTTP/2

---

**24.** Яка мінімальна версія TLS вважається безпечною та прийнятною у 2026 році?

- A) `SSL 3.0`
- B) `TLS 1.0`
- C) `TLS 1.1`
- D) `TLS 1.2`

---

## БЛОК 11 — Балансування навантаження та проксі

**25.** Який алгоритм балансування навантаження найкраще підходить для тривалих з'єднань з різним часом обробки (наприклад, file upload або WebSocket)?

- A) Round Robin
- B) Weighted Round Robin
- C) Least Connections
- D) URL Hash

---

**26.** В чому принципова різниця між Forward Proxy та Reverse Proxy?

- A) Forward proxy прискорює доступ до інтернету, reverse proxy — до локальних ресурсів
- B) Forward proxy відомий клієнту та ховає клієнта від сервера; reverse proxy відомий backend-серверам та ховає їх від клієнта
- C) Forward proxy працює на рівні L7, reverse proxy — на рівні L4
- D) Forward proxy тільки кешує дані, reverse proxy тільки балансує навантаження

---

## БЛОК 12 — HAProxy та конфігурація

**27.** Яка команда перевіряє синтаксис файлу конфігурації HAProxy без його запуску?

- A) `haproxy --test`
- B) `haproxy -v`
- C) `haproxy -c -f /etc/haproxy/haproxy.cfg`
- D) `haproxy --validate`

---

## БЛОК 13 — Системи моніторингу

**28.** Яка модель збору метрик використовується в Prometheus?

- A) Push — агенти самі надсилають метрики на сервер
- B) Pull — сервер сам опитує цілі (targets) за розкладом
- C) Гібридна — Push для метрик, Pull для логів
- D) Event-driven — метрики надсилаються тільки при зміні стану

---

**29.** Який тип метрики Prometheus призначений для монотонно зростаючого лічильника (наприклад, загальна кількість HTTP-запитів)?

- A) Gauge
- B) Histogram
- C) Summary
- D) Counter

---

**30.** На якому TCP-порту за замовчуванням слухає `node_exporter`?

- A) `9090`
- B) `9093`
- C) `3000`
- D) `9100`

---

*Технології системного адміністрування · Курс 2-й · ВІТІ · 2026*
